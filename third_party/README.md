# Third-Party Runtime Data

This folder is for local runtime stores used by the real GraphRAG stack:

- `qdrant/` for Qdrant vector storage
- `neo4j/` for Neo4j Community data/logs
- `keyword_index/` for the local BM25 chunk index

The contents are ignored by Git because they are generated from the local standards corpus.
